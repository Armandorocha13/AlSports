/**
 * Lifecycle hooks para o Content Type Pedido
 * 
 * Nota: ItensPedido é um componente repetível, então o Strapi gerencia automaticamente
 * Não é necessário fazer parse manual dos dados
 */

export default {
  // Lifecycle hooks podem ser adicionados aqui se necessário no futuro
};
